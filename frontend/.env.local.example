# Browser-facing API base URL (must be reachable from the visitor's machine)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Optional: base URL used for server-side rendering of /r/[id].
# Defaults to NEXT_PUBLIC_API_BASE_URL when unset.
# API_BASE_URL=http://127.0.0.1:8000
